import styled from "styled-components";

const Wrapper = styled.div`
	display: flex;
	flex-direction: row;
	align-items: center;
	.user {
		img {
			width: 40px;
			border-radius: 50%;
		}
		margin-right: 1em;
	}
	margin: 1em 0;
`;

const Footer = () => {
	return (
		<Wrapper>

		</Wrapper>
	);
}

export default Footer;